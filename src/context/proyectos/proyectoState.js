import React from 'react';

import proyectoContext from './proyectoContext'


const proyectoState = props => {

    const initialState = {
        
        nuevoProyecto: false // Cuando cambie a true, se mostrará  presionando el boton de nuevo proyecto, el input y el boton de agrega proyecto 
    }
}